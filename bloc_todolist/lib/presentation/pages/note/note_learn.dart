import 'package:flutter/cupertino.dart';

class NoteLearnPage extends StatelessWidget {
  const NoteLearnPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Note Learn'),
    );
  }
}
