import 'package:flutter/material.dart';

class NoteGamePage extends StatelessWidget {
  const NoteGamePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Note Game'),
    );
  }
}
